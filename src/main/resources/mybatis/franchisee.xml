<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="franchisee">
	<!-- 회원가입 -->
	<insert id="add">
		INSERT INTO franchisee (franchisee_code, id, pw, name, birth, sex, tel, address, time)
		VALUES (FRANCHISEE_CODE_SEQ.nextval, #{id}, #{pw}, #{name}, #{birth}, #{sex}, #{tel}, #{address}, sysdate)
	</insert>
	<!-- 중복확인 -->
	<select id="idCount" parameterType="String" resultType="Integer">
		SELECT COUNT(*) FROM franchisee WHERE id = #{franchisee_id}
	</select>
	<!-- 로그인 -->
	<select id="check" parameterType="HashMap" resultType="Franchisee">
		SELECT * FROM franchisee WHERE id=#{id} AND pw=#{pw}
	</select>
</mapper>