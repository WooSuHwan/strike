<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="franchiseePage">
	<select id="game" resultType="Game">
		SELECT * 
		FROM game
		WHERE loc = #{address}
	</select>
	
	<select id="franchiseeGame" resultType="FranchiseeGame">
		select g.game_code, g.member_code, c.member_code
		from game g, challenger c
		where g.game_code = c.game_code
		and g.game_code = #{game_code}
		and c.state = 1
	</select>
</mapper>