<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="challenger">
	<select id="list" resultType="Challenger">
		SELECT *
		FROM member m, game g, challenger c
		WHERE m.member_code = c.member_code AND g.member_code = c.member_code AND
		m.member_code = #{member_code} AND g.game_code = #{game_code};
	</select>
</mapper>