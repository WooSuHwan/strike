<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="franchiseeAdd">

<sql id="search">
		SELECT * FROM franchisee_suggest
		<where>
			<if test="keyword != null">
				name LIKE CONCAT(CONCAT('%', #{keyword}), '%')
			</if>
		</where>
	</sql>
	
	<select id="list" resultType="FranchiseeAdd">
		select *
		from (SELECT ROWNUM rnum, sub.* FROM
		(<include refid="search"></include>) sub)
		<![CDATA[ 
		WHERE rnum >= ((#{page} - 1 ) * #{perPage} + 1) and rnum <= (#{page} * #{perPage})]]>
	</select>
	
	<insert id="add">
		INSERT INTO franchisee_suggest
		VALUES (
		FRANCHISEE_SUGGEST_CODE_SEQ.nextval,
		#{name}, 
		#{address}, 
		#{tel},
		sysdate
		)
	</insert>
	
	<select id="item" resultType="FranchiseeAdd">
		select *
		from franchisee_suggest
		where franchisee_suggest_code = #{franchisee_suggest_code}
	</select>
	
	<!-- pager -->
	<select id="total" resultType="Integer">
		SELECT COUNT(*) FROM (<include refid="search"></include>) sub
	</select>
</mapper>